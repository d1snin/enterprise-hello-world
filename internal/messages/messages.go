package messages

// CommonMessage is a struct that contains only one field representing a message.
type CommonMessage struct {

	// A message. JSON property: "message"
	Message string `json:"message"`
}
